'use strict'

const { shuffle } = require('lodash')
const UserAgent = require('user-agent')

const [ minMs, hourMs, dayMs ] = [60000, 3600000, 86400000]

const ig = async (settings) => {
    
    const {
        instagramBaseUrl = 'https://wwww.instagram.com/',
        
    }

}

module.exports = ig